# 当前工作板

这个文件是项目小白板，用来记录当前谁在做什么。它不是正式需求文档，只用于减少多线程协作时的混乱。

## 进行中

- 玩法代码线程：第二版代码拆分已完成 `levels.js` 和 `board-core.js` 第一轮接入。
- 关卡编辑器线程：已切入 `10×16` 棋盘，后续继续优化编辑体验和关卡生成质量。

## 已确认方向

- 总控负责复杂任务拆分、最终检查、推送、打包和线程同步。
- `旧UI归档｜第一版视觉` 已归档，不再负责第二版主线。
- `美术UI二版｜视觉资产` 负责第二版 UI 的生图和接图，但先做资产预览，不直接污染正式页面。
- 音效声音线程负责音效素材、触发时机、音量层级和手机听感。
- 当前是网页原型，后续目标是微信小游戏；所有新功能默认按手机竖屏、轻量加载、触屏操作来判断。
- 正式棋盘已切换为 `10×16`，四角圆弧保留，外轮廓视觉大小基本不变，让格子和小猪变大。
- 正式关卡当前为 15 关，从 12 只逐步增加到 45 只。
- 关卡编辑器本机主流程：用专用本地服务启动，试玩后点击“部署正式”写入 `levels.js`；远程备用流程：导出给总控。
- GitHub 推送已切到 SSH。
- 多线程预览使用固定端口池：总控 `8030`，玩法 `8031`，UI `8032`，关卡编辑器 `8033`，音效 `8034`。

## 等待确认

- 第二版 UI 美术资产清单。
- 家庭远程验收 15 关难度和小猪大小。
- 家庭试玩后对 15 关难度、星级标准和小猪大小的反馈。

## 暂不做

- 编辑器直接写 GitHub 仓库。
- 编辑器直接发布正式关卡。
- 单独开生图线程。
- 一次性大拆 `game.js`。
- 继续使用旧 `12×19` 关卡作为正式关卡。
